// eslint-disable-next-line no-unused-vars
import { getSafeBody, parseAPIGWResponse } from './common'

describe('getSafeBody', () => {
  it.todo('todo getSafeBody test')
})

describe('parseAPIGWResponse', () => {
  it.todo('todo getSafeBody test')
})
